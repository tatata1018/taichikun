image: python:3.9

pipelines:
  default:
    - step:
        name: SQL Test
        script:
          - pip install duckdb
          - python -c "
import duckdb
cnt = duckdb.query(open('test.sql').read()).fetchone()[0]
print('NG件数:', cnt)
import sys
sys.exit(1) if cnt != 0 else print('OK')
"